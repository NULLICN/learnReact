import {useRef} from "react";

export default function RefTimeout() {
    let timer = useRef(null);
    function startTimer() {
        
    }
    return(
        <>
            <section>
                <p>ref管理计时器</p>
            </section>
        </>
    )
}